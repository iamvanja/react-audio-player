import React from 'react';

import PlayerContainer from '../containers/playerContainer';

const App = () => {
    return (
        <div className="app">
            <PlayerContainer />
        </div>
    );
};

export default App;
